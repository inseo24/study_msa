server:
  port: 8761

spring:
  application:
    name: discovery-test

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    prefer-ip-address: true

management:
  endpoint:
    web:
      exposure:
        include: health